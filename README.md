```mermaid
flowchart LR;
    A[生徒の結果] --> B{レベル判定};
    B -->|ロウ| C[直接的スタイル];
    B -->|ハイ| D[非直接的スタイル];
    C --> E{方法判定};
    D --> E;
    E -->|概念的| F[模範例取得];
    E -->|戦略的| F;
    F --> G[最終プロンプト作成];
    G --> H[API呼び出し];
    H --> I[ヒント返却];
